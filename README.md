# HEGEMON

Territorial conquest with terrain, density, forts and fleets.

Open `index.html` in a browser, or:

```bash
python3 -m http.server 8080
```

Click unclaimed land to found a capital. Attack neighbors with the % bar. `F` fort, `B` fleet, `P` pause.

Win by last banner standing or 72% of claimed land.
